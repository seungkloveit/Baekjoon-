import java.util.Scanner;

public class Test {
	public static void main(String[] args) {
	    Scanner sc = new Scanner(System.in);
	     
	    int n= sc.nextInt(); // 사용자로부터 값을 입력받는다. 
	    sc.close(); // Scanner 클래스를 닫는다. 
	    int sum=0 ; // 합계를 저장할 sum 지역변수를 선언하고, 0으로 초기화해준다. 0으로 초기화하지 않을 시 에러가 발생한다. 
	            
	    for (int i = 1; i <= n; i++) { // 인덱스 1부터 사용자가 입력한 n까지 for문을 돌린다.  
	    	sum += i; // 위에서 선언한 sum 변수에다가 i번째 인덱스 값을 더한 만큼 저장하고 sum 변수에 다 더한 값을 저장한다. 
	    	}
	    System.out.println(sum); // 최종적으로 sum을 출력한다. 
	    }
	  }
